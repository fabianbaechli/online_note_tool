export default class Note {
  constructor(id, title, created, modified, content) {
    this.id = id
    this.title = title
    this.created = created
    this.modified = modified
    this.content = content
  }
}
